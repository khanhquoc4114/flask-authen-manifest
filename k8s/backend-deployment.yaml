apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-backend
  namespace: flask-authen
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flask-backend
  template:
    metadata:
      labels:
        app: flask-backend
    spec:
      containers:
      - name: backend-container
        image: wuoc4114/flask-authen-backend:8b6811ab3270db1f471e28738f52bf7bdf7734f5
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        envFrom:
        - secretRef:
            name: app-secret
---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: flask-authen
spec:
  type: ClusterIP
  selector:
    app: flask-backend
  ports:
    - port: 5000
      targetPort: 5000