apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-frontend
  namespace: flask-authen
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flask-frontend
  template:
    metadata:
      labels:
        app: flask-frontend
    spec:
      containers:
      - name: frontend-container
        image: wuoc4114/flask-authen-frontend:8b6811ab3270db1f471e28738f52bf7bdf7734f5
        imagePullPolicy: Always
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: flask-authen
spec:
  type: LoadBalancer
  selector:
    app: flask-frontend
  ports:
    - port: 80
      targetPort: 80